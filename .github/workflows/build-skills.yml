name: Build and Release Skills

on:
  push:
    branches:
      - main

jobs:
  build:
    permissions:
      contents: write
    # Use the reusable workflow from your toolkit repo
    # Format: {owner}/{repo}/{path}@{ref}
    uses: jaxnext/skill-toolkit/.github/workflows/build-skills.yml@main
